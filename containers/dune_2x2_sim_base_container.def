BootStrap: docker
From: almalinux:9

%post
    # install epel
    dnf install -y epel-release

    # install apptainer
    dnf install -y apptainer

    # install utilities
    dnf install -y mlocate
    updatedb

    # configure local user
    useradd -u 36641 coimbra
